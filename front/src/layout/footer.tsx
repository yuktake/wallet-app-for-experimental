import React from 'react';
import '../css/Footer.css'; // Headerコンポーネントに対応するCSSファイルをインポート

const Footer: React.FC = () => {
    return (
        <footer className="footer">
            footer
        </footer>
    );
};

export default Footer;